bind status P !git push origin
bind status F ?!git push -f origin
bind status a !git commit --amend

bind generic e @nvr -l +%(lineno) %(file)
bind generic b @hub browse
bind generic c @hub compare

# Area                  Foreground      Background         Options
color author            228             default
color cursor            232             141
color date              215             default
color default           253             0
color delimiter         default         default
color header            default         default
color mode              default         default
color section           default         default
color status            default         default
