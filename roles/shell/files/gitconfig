[init]
[user]
	name = Gabriel Poça
[push]
	default = current
[color]
  ui = true
[core]
  excludesfile = ~/.gitignore
  editor = nvr
  attributesfile = ~/.gitattributes
  pager = delta
[github]
  user = gabrielpoca
[commit]
  template = ~/.gitcommit
[alias]
  a = add
  b = branch
  c = checkout
  cb = checkout -b
  co = commit
  conflicts = diff --name-only --diff-filter=U
  d = diff
  f = fetch
  lg = log --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cd) %C(bold blue)<%an>%Creset' --graph --date=human
  p = push -u
  r = rebase
  rc = rebase --continue
  s = status
