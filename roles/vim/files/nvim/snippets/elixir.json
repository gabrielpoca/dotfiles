{
  "IO": {
    "prefix": "IO.inspect()",
    "body": ["IO.inspect(${0})"],
    "description": "IO.inspect()"
  },
  "|> IO": {
    "prefix": "|> IO.inspect()",
    "body": ["|> IO.inspect(${0})"],
    "description": "|> IO.inspect()"
  }
}
