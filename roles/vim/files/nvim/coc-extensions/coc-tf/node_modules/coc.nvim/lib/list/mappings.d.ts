import { Neovim } from '@chemzqm/neovim';
import { ListManager } from './manager';
import { WorkspaceConfiguration } from '../types';
export default class Mappings {
    private manager;
    private nvim;
    private config;
    private insertMappings;
    private normalMappings;
    constructor(manager: ListManager, nvim: Neovim, config: WorkspaceConfiguration);
    doInsertKeymap(key: string): Promise<boolean>;
    doNormalKeymap(key: string): Promise<boolean>;
    private add;
    private onError;
    private evalExpression;
    private doScroll;
}
