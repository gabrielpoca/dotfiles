import { Buffer, Neovim } from '@chemzqm/neovim';
import { Documentation, Fragment } from '../types';
export default class FloatBuffer {
    buffer: Buffer;
    private nvim;
    private srcId;
    private lines;
    private highlights;
    private chars;
    private positions;
    private enableHighlight;
    width: number;
    constructor(buffer: Buffer, nvim: Neovim, srcId: number);
    getHeight(docs: Documentation[], maxWidth: number): number;
    readonly valid: Promise<boolean>;
    readonly highlightOffset: number;
    setDocuments(docs: Documentation[], maxWidth: number): Promise<void>;
    getCodeFragments(fragment: Fragment): Fragment[];
    private fixFiletype;
    readonly height: number;
    setLines(): void;
    private softSplit;
    private getLineCount;
}
