import { Buffer, Neovim } from '@chemzqm/neovim';
import { CancellationToken } from 'vscode-jsonrpc';
import { Chars } from '../model/chars';
import { Documentation, PumBounding } from '../types';
export interface FloatingConfig {
    srcId: number;
    maxPreviewWidth: number;
    chars: Chars;
}
export default class FloatingWindow {
    private nvim;
    private buffer;
    private config;
    private window;
    private bounding;
    private floatBuffer;
    constructor(nvim: Neovim, buffer: Buffer, config: FloatingConfig);
    show(docs: Documentation[], bounding: PumBounding, token: CancellationToken): Promise<void>;
    private showBuffer;
    private calculateBounding;
}
