/// <reference types="node" />
import { NeovimClient as Neovim } from '@chemzqm/neovim';
import { EventEmitter } from 'events';
export default class Plugin extends EventEmitter {
    nvim: Neovim;
    private ready;
    private handler;
    private infoChannel;
    constructor(nvim: Neovim);
    private addMethod;
    init(): Promise<void>;
    findLocations(id: string, method: string, params: any, openCommand?: string): Promise<void>;
    snippetCheck(checkExpand: boolean, checkJump: boolean): Promise<boolean>;
    showInfo(): Promise<void>;
    updateExtension(): Promise<void>;
    cocAction(...args: any[]): Promise<any>;
    dispose(): Promise<void>;
}
