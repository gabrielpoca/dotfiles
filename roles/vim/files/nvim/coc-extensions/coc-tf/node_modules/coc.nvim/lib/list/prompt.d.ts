import { Neovim } from '@chemzqm/neovim';
import { Event } from 'vscode-languageserver-protocol';
import { ListMode } from '../types';
export default class Prompt {
    private nvim;
    private indicator;
    private cusorIndex;
    private _input;
    private timer;
    private _mode;
    private _onDidChangeInput;
    readonly onDidChangeInput: Event<string>;
    constructor(nvim: Neovim);
    input: string;
    mode: ListMode;
    start(input?: string, mode?: ListMode, delay?: boolean): Promise<void>;
    cancel(): void;
    reset(): void;
    drawPrompt(): void;
    moveLeft(): void;
    moveRight(): void;
    moveToEnd(): void;
    moveToStart(): void;
    onBackspace(): void;
    removeNext(): void;
    removeWord(): void;
    removeTail(): void;
    removeAhead(): void;
    insertCharacter(ch: string): void;
}
