# load custon binaries
export PATH="$PATH:$HOME/.dotfiles/bin"

# load antigen
source "$HOME/.antigen/antigen.zsh"

# load custom zsh
source "$HOME/.dotfiles/zsh/plugin.zsh"

# antigen plugins
antigen use oh-my-zsh
antigen theme muse
antigen bundle zsh-users/zsh-syntax-highlighting

antigen apply
