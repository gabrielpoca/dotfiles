#!/usr/bin/env bash
dir=$(find ~/Developer -maxdepth 1 -mindepth 1 -type d | fzf --layout=reverse)
[ -z "$dir" ] && exit 0
name=$(basename "$dir" | tr . _)

if ! tmux has-session -t="$name" 2>/dev/null; then
    tmux new-session -ds "$name" -c "$dir"
fi

tmux switch-client -t "$name"
